/*
	Ursus Quest
	@author Mazen
	
	TODO: Code this. For now just complete it lol.
*/

var status = -1;
var oreArray;

function start(mode, type, selection) {
	qm.forceCompleteQuest();
	qm.dispose();
}

function end(mode, type, selection) {
	qm.forceCompleteQuest();
	qm.dispose();
}