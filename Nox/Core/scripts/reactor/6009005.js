/*
 * FUNC : Oberon Reactor
 */

function act() {
    rm.mapMessage(6, "Breaking small Oberon bx agitated Oberon, and it can no longer control itself. Attacking Oberon has become easier.");
    rm.applyAntiBot_status(9420551);
}